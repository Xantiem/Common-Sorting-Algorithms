void bubble_sort(int *array, int arr_size, bool sort);
void insertion_sort(int *array, int arr_size, bool sort);
void selection_sort(char *array, int arr_size, bool sort);

#include "BubbleSort.h"
#include "InsertionSort.h"
#include "SelectionSort.h"

#define SORT_DECENDING true
#define SORT_ASCENDING false